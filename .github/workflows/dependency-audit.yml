# Managed by mi-examples-workflows. Do not edit by hand: change
# .github/mi-examples-workflows.json and re-run `npx github:mi-examples/mi-examples-workflows`,
# or merge the sync pull request.

name: Dependency audit

# Weekly npm audit of the default branch, to catch advisories published after
# a dependency was installed. Pull requests are audited by CI.
# https://github.com/mi-examples/mi-examples-workflows/blob/main/docs/workflows.md#dependency-audityml

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions: {}

jobs:
  audit:
    name: Dependency audit
    permissions:
      contents: read
    uses: mi-examples/mi-examples-workflows/.github/workflows/dependency-audit.yml@e9dbb3ff76c930e744437d1c52e2654f864c404f # v0.6.0
