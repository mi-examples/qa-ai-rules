# Managed by mi-examples-workflows. Do not edit by hand: change
# .github/mi-examples-workflows.json and re-run `npx github:mi-examples/mi-examples-workflows`,
# or merge the sync pull request.

name: Secret scan

# Shared gitleaks scan: https://github.com/mi-examples/mi-examples-workflows/blob/main/docs/workflows.md#secret-scanyml

on:
  pull_request:
    branches: [main, develop]

permissions: {}

concurrency:
  group: secret-scan-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  secret-scan:
    name: Secret scan
    permissions:
      contents: read
    uses: mi-examples/mi-examples-workflows/.github/workflows/secret-scan.yml@e9dbb3ff76c930e744437d1c52e2654f864c404f # v0.6.0
